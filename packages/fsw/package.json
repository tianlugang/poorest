{
  "name": "@poorest/fsw",
  "version": "2.2.0",
  "description": "wrapper base file opreation.",
  "license": "MIT",
  "files": [
    "lib/*"
  ],
  "main": "lib/index.js",
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "api": "api-extractor run --local --verbose",
    "build": "tsc --build",
    "clean": "rimraf ./lib && rimraf *.tsbuildinfo && rimraf ./types",
    "docs": "typedoc --options tdoptions.json src"
  },
  "dependencies": {
    "@poorest/base": "^2.1.0",
    "@poorest/util": "^2.1.2",
    "graceful-fs": "^4.2.6"
  }
}

{
  "name": "@poorest/markdown",
  "version": "2.1.0",
  "description": "markdwon util",
  "license": "MIT",
  "files": [
    "lib/*"
  ],
  "main": "lib/index.js",
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "api": "api-extractor run --local --verbose",
    "build": "tsc --build",
    "clean": "rimraf ./lib && rimraf *.tsbuildinfo && rimraf ./types",
    "docs": "typedoc --options tdoptions.json src"
  },
  "dependencies": {
    "@poorest/is": "^2.1.0",
    "highlight.js": "^10.7.2",
    "markdown-it": "^4.4.0",
    "xss": "^1.0.8"
  },
  "devDependencies": {
    "@types/markdown-it": "^12.0.1"
  }
}

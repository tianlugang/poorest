import { } from '@/utils/request'
import { _capitalized_State, GetDataModel } from '@/types/pages/_page_/_capitalized_.types'

export const getData = (data: GetDataModel):PromiseLike<any> => {
    return  Promise.resolve({})
}